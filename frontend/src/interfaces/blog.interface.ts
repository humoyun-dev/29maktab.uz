import { CommentInterface } from "@/interfaces/comment.interface";

export interface BlogInterface {
  id: string;
  title: string;
  content: string;
  slug: string;
  image: string;
  video: string;
}
