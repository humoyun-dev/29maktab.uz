/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["api.29maktab.uz", "127.0.0.1", "daisyui.com", "images.unsplash.com"],
  },
};

module.exports = nextConfig;
